import styled from "styled-components";
import { Grid } from "@mui/material";

export const CorrectionAgeContainer = styled(Grid)`
  && {
    max-width: 600px;
  }
`;

export const CorrectionAgeRow = styled.div`
  && {
    display: flex;
    justify-content: space-between;
  }
`;
